// Fill this in with your real deployed Worker URL (from `wrangler deploy`),
// then commit. This is the only file that needs a real value plugged in.
export const APIARY_API = "https://REPLACE-WITH-YOUR-WORKER-SUBDOMAIN.workers.dev";
